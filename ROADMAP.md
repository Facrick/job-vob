# 🗺️ Roadmap — QA Smart Assistant Pro

Документ описывает план развития приложения: фичи и технические доработки,
сгруппированные по фазам и приоритизированные по «эффект / трудозатраты».

> Статус на момент составления: рабочее десктоп-приложение на Flet (CRM-воронка,
> ИИ-письма, mock-интервью, аналитика зарплат, учебник QA на 13 разделов).
> Ядро (`core/`) отделено от UI (`gui/`), есть AppConfig-синглтон, fallback-модель
> Groq, базовый анти-фрод парсинга hh.ru.

---

## 1. Оценка текущего состояния

**Сильные стороны**
- Чистое разделение слоёв: `core/` (логика) ↔ `gui/` (Flet UI + контроллер).
- Устойчивый ИИ-слой: ретраи + резервная модель, единый `_complete()`.
- Учебник вынесен в данные (`data/handbook.json`), есть поиск и рендер кода.
- Фоновые задачи через единый `_run_bg()` (кнопки не «залипают»).

**Слабые места (tech debt)**
- ❌ **Нет тестов** (`tests/` отсутствует), нет CI и линтера, нет `pyproject.toml`.
- ⚠️ **Парсинг через Playwright + обход капчи** — хрупко и на грани ToS hh.ru.
- ⚠️ **Воронка неполная:** статус меняется автоматически только на `processed`
  (после письма) и `applied` (после отклика). Перевести вакансию в
  `interview / offer / rejected` из UI **нельзя**.
- ⚠️ **`progress_callback` парсера не подключён** к UI — нет прогресс-бара поиска.
- ⚠️ **Дублируется `handbook.json`** (`data/` и `core/`) — легко рассинхронить.
- ⚠️ Хардкод путей (`data/`, `resume.pdf`, `covers/`) ломает сборку PyInstaller
  (`run.spec`, `dist/` уже есть); нет resource-resolver под frozen-режим.
- ⚠️ Резюме только из `resume.pdf` в корне — нет управления из UI.
- ⚠️ UI-обновления из фоновых потоков идут напрямую `page.update()` (работает,
  но не гарантировано потокобезопасно для всех виджетов).

---

## 2. Принципы приоритизации

1. **Сначала надёжность, потом фичи.** Без тестов любая новая фича повышает риск.
2. **Снизить юридический/технический риск** скрейпинга (официальный API hh.ru).
3. **Закрывать «обрезанные» сценарии**, которые уже частично есть в данных
   (воронка, прогресс), — высокий эффект при малых трудозатратах.
4. **Не раздувать.** Каждую фичу проверять на реальном UI (превью-скриншоты).

---

## 3. Дорожная карта по фазам

### ✅ Фаза 1 — Стабилизация и качество (фундамент) — ВЫПОЛНЕНО
Цель: сделать кодовую базу безопасной для изменений.

- **✅ F1.1 Тесты ядра (pytest).** `tests/`: 26 тестов — `parse_salary_text`,
  `extract_salary_from_resume`, `html_to_markdown` (вкл. `<pre>`),
  `VacancyRepository` (temp sqlite) + CSV-экспорт, `VacancyStatus`,
  целостность учебника. _Побочно найден и исправлен баг: «Зарплата: 180 000»
  не извлекалась из резюме._
- **✅ F1.2 CI + линт.** `.github/workflows/ci.yml` (ruff + pytest на push/PR),
  `pyproject.toml` (конфиг pytest/ruff), `requirements-dev.txt`. Ruff — чисто.
- **✅ F1.3 Единый источник учебника.** `core/handbook.json` удалён; loader
  использует `resource_path()` и находит `data/handbook.json` в dev и frozen.
- **✅ F1.4 Resource-resolver.** `core/paths.py` (`resource_path` для bundled,
  `user_path` для записываемых). Подключён в `QAHandbook`, `VacancyRepository`,
  `AppConfig`, логах.
- **◻️ F1.5 Потокобезопасные UI-апдейты.** Частично: UI-апдейты централизованы
  в `_run_bg()` (единая точка) и защищены в `FletLogHandler`. Полноценный
  marshalling на event-loop Flet вынесен в бэклог (текущая схема работает).

### ✅ Фаза 2 — Завершение ключевых сценариев — ВЫПОЛНЕНО
Цель: довести до конца то, что уже частично заложено в данных.

- **✅ F2.1 Полноценная Kanban-воронка.** Выпадающий список «Этап воронки» в
  панели деталей переводит вакансию по любому статусу (`discovered → … →
  offer/rejected`); цветные счётчики по этапам над таблицей обновляются.
- **✅ F2.2 Прогресс-бар поиска.** `progress_callback` парсера подключён к
  `ft.ProgressBar` в карточке поиска (прогресс по детальному парсингу).
- **✅ F2.3 Управление резюме из UI.** Кнопка «Резюме» (`ft.FilePicker`,
  PDF) копирует файл в `user_path("resume.pdf")`, сбрасывает кэш текста,
  показывает имя активного резюме.
- **✅ F2.4 Экспорт с выбором пути.** «Экспорт CSV» через `FilePicker.save_file`
  (диалог сохранения) вместо фиксированного пути. _Экспорт .zip/.xlsx — бэклог._
- **✅ F2.5 Заметки по вакансии.** Поле «Заметки» в панели деталей, авто-сохранение
  в БД (`update_notes`) при потере фокуса. _Дедлайны/напоминания — бэклог._

### 🟨 Фаза 3 — Снижение риска парсинга (в работе)
Цель: уйти от хрупкого скрейпинга.

- **✅ F3.1 Официальный API hh.ru (с авто-фолбэком).** `core/api_client.py`
  (`HHApiClient` на stdlib `urllib`, без новых зависимостей) + `core/search_service.py`
  (`SearchService`): API — основной источник, при ошибке/пустом ответе ПРОЗРАЧНО
  используется Playwright-парсер. Флаг `use_official_api` (default True).
  Маппинг JSON→dict и логика фолбэка покрыты тестами (11 шт., офлайн).
  _Примечание: из дата-центров hh.ru может отдавать 403 — тогда срабатывает
  фолбэк; с обычной (резидентной) сети API работает._
- **◻️ F3.2 OAuth hh.ru для откликов.** Легальные отклики через API вместо
  автоматизации браузера. — _бэклог._
- **◻️ F3.3 Кэш и квоты.** Rate-limit, кэш ответов, ретраи с backoff. — _бэклог._

### 🟩 Фаза 4 — Усиление ИИ-возможностей
Цель: повысить ценность ИИ-функций.

- **✅ F4.1 Match-score резюме↔вакансия + умная лента.** `core/matching.py`
  (быстрый офлайн-скор по пересечению навыков, без LLM), колонка `match_score`
  в БД + миграция, сортировка ленты по убыванию релевантности, цветные %-чипы,
  показ в деталях, пересчёт при смене резюме. Покрыто тестами (7 шт.).
  Глубокий ИИ-разбор (`analyze_vacancy`) остаётся по клику.
- **F4.2 Адаптация резюме под вакансию.** ИИ предлагает правки/акценты в резюме.
- **F4.3 Голосовое/текстовое mock-интервью с финальным фидбеком** и оценкой по
  компетенциям; сохранение истории и прогресса.
- **F4.4 Настройка тона/языка письма** (формальный/дружелюбный, RU/EN).
- **F4.5 Выбор LLM-провайдера** (Groq/OpenAI/локальный) через конфиг.

### 🟦 Фаза 5 — Аналитика и масштаб
- **F5.1 Дашборд воронки:** конверсия по этапам, среднее время до отклика/оффера.
- **F5.2 Аналитика рынка:** динамика зарплат по времени, топ-навыки из описаний
  (ИИ-агрегация), сравнение с ожиданиями пользователя.
- **F5.3 Мультипользовательский режим / облачная синхронизация** (опционально).

---

## 4. Технические доработки (tech debt)

| ID | Доработка | Зачем |
|----|-----------|-------|
| T1 | Тесты + CI + ruff + `pyproject.toml` | Безопасность изменений |
| T2 | Убрать дубль `handbook.json` | Единый источник правды |
| T3 | `resource_path()` для frozen-сборки | Рабочий `.exe` |
| T4 | Потокобезопасные UI-апдейты | Стабильность под нагрузкой |
| T5 | Типизация (mypy) + dataclass `Vacancy` вместо «голых» dict | Надёжность |
| T6 | Логи: ротация и уровень из конфига; не спамить `page.update()` | Производительность |
| T7 | Слой репозитория: пул соединений/контекст-менеджер | Корректность БД |
| T8 | Вынести строковые статусы на `VacancyStatus` повсеместно | Меньше magic strings |
| T9 | Версионирование схемы БД + миграции | Безопасные апгрейды |
| T10 | Сборка: обновить `run.spec`, очистить `build/`, артефакт в CI | Дистрибуция |

---

## 5. Матрица приоритетов (эффект × трудозатраты)

```
Эффект ▲
ВЫСОКИЙ │  F2.1 Воронка        F1.1 Тесты         F3.1 API hh.ru
        │  F2.2 Прогресс       F1.3 Дедуп учебн.  F4.1 Match-score
        │  T2  Дедуп           F1.2 CI/линт
СРЕДНИЙ │  F2.4 Экспорт        F2.3 Резюме UI     F4.2 Адаптация рез.
        │  F2.5 Заметки        T3 resource_path   F5.1 Дашборд
        │  T8 статусы-enum     T4 потоки
НИЗКИЙ  │  T6 логи             F4.4 тон письма    F5.3 облако
        └───────────────────────────────────────────────────────▶
           МАЛЫЕ                СРЕДНИЕ              БОЛЬШИЕ   Трудозатраты
```

**Рекомендуемый порядок (первые 6 шагов):**
1. F1.1 Тесты ядра → 2. T2 Дедуп учебника → 3. F2.1 Полная воронка →
4. F2.2 Прогресс-бар → 5. F1.2 CI/линт → 6. F3.1 Пилот API hh.ru.

---

## 6. Риски и ограничения

- **ToS hh.ru.** Скрейпинг + обход капчи + авто-отклик могут нарушать правила.
  Митигация: фаза 3 (официальный API/OAuth), пометки в UI, режим «только ручной отклик».
- **Секреты.** `GROQ_API_KEY` в `.env` (в `.gitignore`). Не коммитить; ротация при утечке.
- **Зависимость от модели Groq.** Уже есть fallback-модель; добавить выбор провайдера (F4.5).
- **Хрупкость UI Flet** при крупных рефакторах вёрстки — проверять превью-скриншотами.

---

## 7. Метрики успеха

- Покрытие тестами ядра ≥ 70%; зелёный CI на каждый PR.
- 0 «серых экранов» / необработанных исключений в типовых сценариях.
- Доля успешных поисков без ручного вмешательства (капча) ↑ (после F3.1).
- Время «вакансия → готовое письмо» ↓; конверсия воронки видна в дашборде (F5.1).

---

*Документ живой — обновляйте статусы (TODO / In progress / Done) по мере реализации.*
